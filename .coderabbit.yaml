language: en-US
tone_instructions: Be concise and direct.

reviews:
  path_instructions:
    - path: "src/billing/**"
      instructions: "Check rounding and currency handling carefully."
    - path: "**/*.test.ts"
      instructions: "Flag tests asserting on implementation details."
  path_filters:
    - "!dist/**"
    - "!**/*.generated.ts"
    - "src/**"
    - "!vendor/{a,b}/**"
  auto_review:
    enabled: true

code_generation:
  docstrings:
    language: en-US
